{
  "name": "teslacost",
  "version": "0.1.0",
  "private": true,
  "description": "Tesla cost tooling monorepo — Canada finance/lease/cash instrument + US EV-switch cost model.",
  "packageManager": "pnpm@11.8.0",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "test": "turbo run test",
    "typecheck": "turbo run typecheck"
  },
  "devDependencies": {
    "turbo": "^2",
    "typescript": "^5.7.3"
  }
}
